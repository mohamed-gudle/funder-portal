export default function OverviewPage() {
  // This page component is required for the parallel routes to work
  // The actual content is rendered in the layout.tsx via parallel routes
  return null;
}
